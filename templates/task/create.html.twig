{% extends 'base.html.twig' %}

{% block title %}
    Create Task
{% endblock %}

{% block body %}

    <div class="card">
        <div class="card-body">
            {{ form_start(form) }}
            {{ form_widget(form) }}
            <button type="submit" class="btn btn-success">Create</button>
            {{ form_end(form) }}
        </div>
    </div>

{% endblock %}